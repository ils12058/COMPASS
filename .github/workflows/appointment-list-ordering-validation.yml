name: Appointment list ordering validation

on:
  push:
    branches:
      - feature/appointment-list-ordering
    paths:
      - "be/**"
      - ".github/workflows/appointment-list-ordering-validation.yml"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: compass_test
          POSTGRES_USER: compass
          POSTGRES_PASSWORD: test-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U compass -d compass_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:8
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install locked dependencies
        working-directory: be
        run: uv sync --frozen
      - name: Focused static and migration checks
        working-directory: be
        run: |
          uv lock --check
          uv run ruff format --check compass/appointments tests/test_appointments.py tests/test_openapi_contract.py
          uv run ruff check compass/appointments tests/test_appointments.py tests/test_openapi_contract.py
          uv run python -m compileall -q compass/appointments tests/test_appointments.py tests/test_openapi_contract.py
          uv run python manage.py makemigrations --check --dry-run --settings=tests.settings
          uv run python manage.py check --settings=tests.settings
      - name: Targeted Appointment tests
        working-directory: be
        run: uv run pytest tests/test_appointments.py
      - name: Generate canonical OpenAPI
        working-directory: be
        run: uv run python manage.py export_openapi --settings=tests.settings
      - name: Targeted OpenAPI checks
        working-directory: be
        run: >-
          uv run pytest
          tests/test_openapi_contract.py::test_all_public_operations_have_stable_unique_ids_and_approved_tags
          tests/test_openapi_contract.py::test_appointment_frontend_readiness_openapi_contract
          tests/test_openapi_contract.py::test_appointment_list_ordering_openapi_contract
      - name: Diff whitespace check
        run: git diff --check
      - name: Sync generated OpenAPI contract
        run: |
          if git diff --quiet -- contracts/openapi.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add contracts/openapi.json
          git commit -m "chore: sync OpenAPI contract"
          git push
